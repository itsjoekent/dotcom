{
  "private": true,
  "scripts": {
    "start": "run-p start:server build:watch",
    "start:server": "PORT=5000 ts-node --esm src/server.ts",
    "build": "tsc && vite build",
    "build:watch": "vite build --watch",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/express": "^4.17.16",
    "express": "^4.18.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.3",
    "vite": "^4.0.0"
  },
  "dependencies": {
    "@types/node": "^18.11.18",
    "npm-run-all": "^4.1.5"
  }
}
